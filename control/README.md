# control

Cohort control plane: members, signup, Stripe SetupIntents and the
threshold charge, key issue and revocation, plans and seats. Postgres.
Payments sit behind a thin interface (ADR 004). Never on the inference
path.
