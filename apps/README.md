# apps

site/ is the marketing site (site-kit, static, no backend). portal/ is
one Next.js app, role-split: members see usage, keys, budgets, and queue
state; admins see members, charges, node state, key kill, and admission
knobs. Built after the serving path works.
